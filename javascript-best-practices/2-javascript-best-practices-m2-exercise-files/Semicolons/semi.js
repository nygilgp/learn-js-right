var a = 10;

var js = 12;




function test() {
  let x = 1;
  if (x ===true) {
    
  }

  x += 1; // Default: 'x' used out of scope.
            // No warning when funcscope:true

  return {
    x: x,
    y: y 
  };
}
